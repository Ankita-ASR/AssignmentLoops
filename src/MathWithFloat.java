/*import java.util.Scanner;

public class MathWithFloat {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        float array[] = new float[n];
        float sum = 0.0f;
        for (int i = 0; i < array.length; i++) {
            array[i] = scanner.nextFloat();
            sum = sum + array[i];
        }
      double avg = sum / array.length; //the average of the values.
        //the smallest of the values• the largest of the values
        float min = array[0];
        float max = array[0];
        for (int i = 1; i < array.length; i++) {
            if (array[0] > array[i]) ;
            min = array[i];
            if (array[0] < array[i]) ;
            max = array[i];
        }
        System.out.printf("smallest value:%f \n largest value:%f",min,max);

    }
}*/
